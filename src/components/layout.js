import React, { Fragment } from 'react'

export default function layout({ children }) {
    return (
        <Fragment>
            {
                children
            }
        </Fragment>
    )
}
